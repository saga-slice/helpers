// import { createModule } from '../createModule';

test('should test', () => {

    // [
    //     [],
    //     "",
    //     123,
    //     false,
    //     null,
    //     undefined,
    //     () => {}
    // ].forEach(opts => {

    //     expect(() => createModule(opts)).toThrow(/options.+must be.+object/);
    // })
});
