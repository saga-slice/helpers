// import ApiIndex from '../';

describe('api index', () => {
    test('API Index: should not allow bad options', () => {

        // expect(() => {
        //     sagaHelpers.crudSaga();
        // }).toThrow(/options\.singular.+required/);
    });
});